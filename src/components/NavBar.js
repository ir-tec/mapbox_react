import { AppBar, Toolbar } from "@material-ui/core";
import React from "react";

const NavBar = () => {
  return (
    <AppBar>
      <Toolbar></Toolbar>
    </AppBar>
  );
};

export default NavBar;
